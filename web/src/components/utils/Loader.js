import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

const Loader = () => {
  return <CircularProgress style={{ marginLeft: '40%', marginTop: '20%' }} />;
};

export default Loader;
